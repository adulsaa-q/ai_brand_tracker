# Runtime + dev dependencies are declared in pyproject.toml (PEP 621).
#
#   pip install -e ".[dev]"            # core + test tooling
#   pip install -e ".[dev,dashboard]"  # also the Streamlit dashboard
#
# This file is kept only so tools that expect requirements.txt do not break.
-e .
